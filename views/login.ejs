<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/styles/login.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css">

</head>

<body>
    <div class="login-container mt-5">
        <h1>
            <%=heading%>
        </h1>
        <form method="POST" action=<%=action%>>
            <%if(err){%>
                <div class="alert alert-danger" role="alert">
                    <%=err%>
                </div>
                <%}%>

                    <label>
                        username
                    </label>
                    <input type="text" placeholder="Enter your username or email" name="username">

                    <label>Password:</label>
                    <input type="password" placeholder="Enter your password" name="password">

                    <button type="submit" class="login-btn mt-3 me-2">Log In</button>
        </form>

        <%if(flag) {%>
            <p>Don't have an account? <a href='/auth/register'>Sign up</a></p>
            <%}%>

                <%if(flag) {%>
                    <a href="/auth/admin/login" class="btn btn-secondary mt-3 ms-2" style="text-decoration: none;">Log
                        In
                        As
                        Admin</a>
                    <%}%>
    </div>


</body>

</html>